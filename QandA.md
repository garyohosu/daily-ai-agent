# QandA.md - 仕様レビュー QA シート
Version: 0.5
対象: SPEC.md v0.2-draft
作成: 2026-03-12
更新: 2026-03-13

---

## Q01. Grokタスクの送信元メールアドレスは確定か？
**セクション**: 10.3, 11.2
**質問**: `From: noreply@x.ai` と記載があるが、実際に確認済みか？送信元が異なる場合、Gmailフィルタ条件全体に影響する。
**回答**: 実受信メールにより `Grok <noreply@x.ai>` からの通知を確認済み（2026-03-13）。Gmail 検索条件の第一候補は `from:noreply@x.ai` を正式採用とする。さらに今回の実機確認で、メール本文が `Title:` / `Summary:` / `Why it is trending:` / `X URL:` のラベル形式で構造化されていること、`https://x.com/...` の URL が含まれること、受信時刻が Grok タスク設定の 06:30 と一致することも確認済み。Phase 0 の初期検索条件・解析方式の両方に直接使える証拠として扱う。

---

## Q02. GrokタスクはどのX/Grokアカウント・プランで使うか？
**セクション**: 9.1
**質問**: Grokのタスク機能はX Premium+（有料）が必要と思われるが、利用するアカウントとプランは確定しているか？また、タスクの実行時刻（06:30 JST）を指定できるUIや機能がGrok側に存在するか？
**回答**: X の課金ユーザー環境で Grok のタスク機能が利用可能であることを、実機画面で確認済み。少なくとも本プロジェクトで使用する運用アカウントではタスク機能が使える。なお、一般論としてどの課金プラン全体で使えるかまでは本仕様では保証せず、本運用アカウントで使用可能であることを要件充足条件とする。

---

## Q03. Grokタスクの自動実行メカニズムは何か？
**セクション**: 9.4
**質問**: 「毎朝06:30 JST実行」とあるが、Grokタスクのスケジューリングはどのように行うのか？Grok UI上のスケジュール設定か、外部スクリプトからAPIを叩くのか。
**回答**: 06:30 JST の定期実行は、Grok のタスク機能そのもので設定可能であり、実際に「毎日 6:30」の設定画面を確認済み。したがって初期版では、外部スケジューラを使わず、Grok タスクの内蔵スケジュール機能を正式採用する。

---

## Q04. Gmail APIの認証方式は何を使うか？
**セクション**: 11.3, 12.1
**質問**: Gmail取得にGmail APIを使う想定と思われるが、認証はOAuth2（ユーザー認証）かService Accountか？資格情報の管理・保管方法はどうするか？
**回答**: 認証方式は OAuth2 を採用する。Service Account は個人 Gmail 直読の初期構成には適さないため不採用とする。資格情報はローカル開発では `credentials.json` とトークンキャッシュを `.gitignore` 管理し、GitHub 等へは絶対にコミットしない。Phase 2 以降に自動化基盤へ移す場合は secrets 管理へ移行する。

---

## Q05. 「未処理」の状態管理はどうするか？
**セクション**: 11.3
**質問**: 「まだ未処理である」メールを判定する仕組みとして、Gmailラベル・既読/未読フラグ・保存済みmessage_idのどれを使うか？
**回答**: 未処理判定は `data/raw/` に保存済みの `message_id` との突き合わせを主判定とする。Gmail ラベルは補助的に用いる。既読/未読フラグは人手操作で変化しやすいため正式判定には使わない。

---

## Q06. メール解析にLLMを使うか？
**セクション**: 12.3, 12.4
**質問**: Grokの出力が毎回完全に構造化されているとは限らない。LLM（Claude API等）を使った解析も選択肢に含めるか？
**回答**: Phase 1 は正規表現とルールベース解析を基本とする。メール書式の揺れが大きい場合に備え、Phase 2 以降で LLM 補助解析をオプション導入できる設計にする。初期実装で Claude API 等を必須依存にはしない。

---

## Q07. Title類似度の閾値と「直近数日」の定義は何か？
**セクション**: 14.2
**質問**: 「Title類似度が高い」の判定閾値はどこで定めるか？「直近数日」は何日か？
**回答**: 初期版では URL 完全一致を最優先とし、タイトル類似度は補助判定とする。タイトル類似度の暫定閾値は 0.85 相当とし、「直近数日」は 7 日と定義する。運用結果を見て調整する。

---

## Q08. 重複判定の参照データはどこに保存するか？
**セクション**: 14.2
**質問**: 重複判定に使う過去X URLの一覧はdata/index.jsonで管理するのか、それとも専用ファイルを設けるか？
**回答**: 重複判定用の過去 URL 一覧は `data/dedupe_index.json` のような専用ファイルで管理する。`data/index.json` は公開記事一覧に集中させる。長期化した場合は日付単位で分割または SQLite 化を検討する。

---

## Q09. 記事生成（編集コメント・総括文）には何のAIを使うか？
**セクション**: 15.6, 15.7
**質問**: 編集コメントと総括文の自動生成にはどのAI/LLMを使うか。初期段階ではルールベースか自動生成どちらを優先するか？
**回答**: Phase 1 では編集コメント・総括文はルールベース優先とし、必要に応じて対話型 AI を使って補助生成する。Phase 2 以降で API 利用を選択可能とするが、初期版では必須要件にしない。

---

## Q10. サイトのフレームワーク・テーマは何か？
**セクション**: 16.3, 17.1, 26（末尾追記）
**質問**: `docs/_posts/` の構造はJekyll想定か？配信ブランチは？「CDNを使ったモダン技術ブログ的外観」の具体的なフレームワーク指定はあるか？
**回答**: `docs/_posts/` 構成を採るため、初期版は Jekyll 前提とする。配信は GitHub Pages の標準構成に合わせ、main ブランチの `/docs` を正式採用とする。デザインはまずシンプルな技術ブログ風に留め、CSS フレームワークは後段で決める。

---

## Q11. GoogleアドセンスはSPEC本文要件として扱うか？
**セクション**: 26（末尾追記）
**質問**: アドセンスのコードがSPEC末尾に記述されているが、これを正式要件とするか？初期公開時から承認済みか？
**回答**: アドセンスは初期公開の必須要件としない。別紙または将来要件として管理し、SPEC 本文の必須機能からは切り離す。承認状態が不明なため、実装優先順位は低とする。

---

## Q12. 記事の言語は日本語か？
**セクション**: 9.3（プロンプト末尾 "Output in Japanese"）
**質問**: Grokの出力は日本語指定だが、記事本文もすべて日本語か？英日混在の方針はあるか？
**回答**: 記事本文は原則として全文日本語とする。英語の固有名詞、製品名、引用元タイトルのみ必要に応じて併記する。英日混在を基本方針にはしない。

---

## Q13. GitHub Actionsや自動実行基盤を使うか？
**セクション**: 19.2, 19.3, 25（Phase 2以降）
**質問**: Gmail取得・記事生成・push・公開の自動化にGitHub ActionsやcronジョブなどのCI/CD基盤を使うか？secretsの管理方針は？
**回答**: Phase 1 はローカル実行＋手動 push を基本とする。Phase 2 以降で GitHub Actions を検討する。secrets は GitHub Actions Secrets を利用し、ローカル credential を直接リポジトリに置かない。

---

## Q14. エラー時の再試行の回数・間隔は？
**セクション**: 20.2
**質問**: 「再試行」とあるが、リトライ回数・間隔の定義がない。
**回答**: Gmail 取得の再試行は 3 回、30 秒間隔を初期値とする。公開処理失敗時は自動再試行せず、人間確認を優先する。

---

## Q15. インデックスロールバックの具体的な方法は？
**セクション**: 20.5
**質問**: 「インデックス更新をロールバック可能にする」の具体的な方式は何か？
**回答**: インデックス更新前にバックアップファイルを一時保存し、公開前の最終単位は Git コミットで管理する。初期版では `index.json.bak` による簡易ロールバックと Git による巻き戻しの併用とする。

---

## Q16. Phase 0の実施者とツールは何か？
**セクション**: 10.1〜10.6
**質問**: Phase 0の実証実験は、手動でGmailを確認するのか、`poc_gmail_read.py` スクリプトを実行するのか？実行環境の制約はあるか？
**回答**: Phase 0 はまず手動確認＋PoC スクリプト実行の二段階で行う。実行環境は Python 3.10 以上を推奨とし、Gmail API 用ライブラリを利用する。人手確認だけで終わらせず、再現可能なスクリプト結果を残す。

---

## Q17. 「続報」判定のルールはいつ定めるか？
**セクション**: 14.3
**質問**: 「続報判定ルールは将来追加」とあるが、追加フェーズと基本方向性は？
**回答**: 続報判定ルールは Phase 2 で追加する。方向性としては、同一主題だが新しい X URL または新しい一次ソースを持つ場合を候補とし、タイトルだけで判定しない。

---

## Q18. Googleアドセンス未承認時も初期公開をブロックするか？
**セクション**: 26（末尾追記）
**質問**: 「googleアドセンスを各ページに入れておくこと」が追加されたが、AdSense 審査未完了・配信不可・ポリシー確認未了の状態でも、初期公開の完了条件に含めるか？含める場合、Phase 0〜3 のどこで満たすべきか？
**回答**: AdSense 未承認時でも初期公開をブロックしない。AdSense は初期公開の完了条件に含めず、将来要件として別管理する。

---

## Q19. 「CDNを使ったモダン技術ブログ的外観」の達成条件は何か？
**セクション**: 26（末尾追記）
**質問**: 「各ページはCDNを使ったモダン技術ブログ的外観のサイトとすること」とあるが、使用対象は CSS のみか、JS ライブラリも含むか。許容する CDN、対象ページ（記事、トップ、about、categories）、および Jekyll/Markdown 構成のどこで読み込むかを定義する必要があるのではないか？
**回答**: 「CDNを使ったモダン技術ブログ的外観」は初期版の正式要件にしない。初期版はシンプルで可読性の高いレイアウトを採用し、CSS フレームワークや CDN 利用は後段で決定する。

---

## Q20. AdSense導入に伴う透明性表示・ポリシー文言は必要か？
**セクション**: 18, 26（末尾追記）
**質問**: 収集・編集方針の透明性表示は定義されているが、第三者広告スクリプトを導入する場合の広告表示方針、プライバシー告知、必要な説明文の掲載場所が未定義である。これらも初期公開要件に含めるか？
**回答**: 広告導入時は、広告表示方針・プライバシー告知・必要な説明文を別途定義する。ただし初期版では広告を必須導入しないため、初期公開要件には含めない。導入時に `privacy` / `about` / `footer` 等への掲載方針を追加定義する。

---

## Q21. サイトのデザイン・レイアウトにおける構成要素は？
**セクション**: 17.3, 17.4
**質問**: 「シンプルな技術ブログ風」としているが、サイドバーの有無、ヘッダー/フッターの固定要素（検索窓、タグクラウド、SNSリンク等）についての具体的な希望はあるか？
**回答**: 初期版はサイドバーなしの1カラム構成またはシンプルな右サイドバー構成とし、ヘッダーにタイトルとナビゲーション（About/Categories）、フッターにポリシーと著作権表示を置く。検索機能やタグクラウドは記事数が増えた段階で検討する。

---

## Q24. スクリプト間のオーケストレーション方法は何か？
**セクション**: 23
**質問**: `fetch_gmail.py → parse_mail.py → normalize_items.py → dedupe_items.py → compose_article.py → publish_site.py` という複数スクリプトの実行順は、どう管理するか？Makefile・シェルスクリプト・単一の `main.py` のいずれを採用するか？Phase 1（ローカル手動実行）と Phase 2（GitHub Actions）で方式が変わる想定か？
**回答**: 正式な実行順序の定義元は `scripts/main.py` とする。`main.py` が `fetch_gmail.py → parse_mail.py → normalize_items.py → dedupe_items.py → compose_article.py → publish_site.py` を順に呼ぶ。Makefile は補助的に使ってよいが、実行順序の正本にはしない。

---

## Q25. draft扱い記事の保存先とフォロー手順は？
**セクション**: 20.3
**質問**: 「記事生成不可なら draft 扱い」とあるが、Section 23 のディレクトリ構成に `data/draft/` が存在しない。draft はどこに保存するか？また draft を確認・再処理する手順（誰がいつ何をするか）は定義しないか？
**回答**: draft は `data/draft/YYYY-MM-DD_<message_id>.json` に保存する。同日複数失敗でも上書きしない。必要なら確認用 Markdown 草稿を `docs/_drafts/` に置いてよいが、初期版の正本は JSON とする。

---

## Q26. `docs/` 配下の Jekyll 設定ファイルの扱いは？
**セクション**: 23, Q10
**質問**: `docs/` 配下のディレクトリ構成に `_config.yml` を置くか？Jekyll のビルドに必須のこのファイル（サイト名・baseurl・URL・パーマリンク設定等）はどこに置くか？また GitHub Pages のビルド対象ブランチ・ルートディレクトリ（`/` か `/docs` か `gh-pages` ブランチ等）はどれを採用するか？
**回答**: 公開方式は main ブランチの `/docs` を正式採用とする。Jekyll 設定ファイルは `docs/_config.yml` を必須とする。最低限 `title` `description` `url` `baseurl` `permalink` を定義する。

---

## Q27. 運用モード移行の判断基準は何か？
**セクション**: 19
**質問**: 手動→半自動→全自動への移行の基準が未定義。「十分安定した場合」とは具体的に何をもって判断するか（例: エラー率、連続稼働日数、品質確認回数、記事件数の閾値等）？
**回答**: 手動 → 半自動は「Gmail 直読 PoC 成功後、7日以上の試験運用で取得・解析・草稿生成の成功率が 90%以上」とする。半自動 → 全自動は「14日以上の連続運用で重大エラー 0件、公開差し止め 1件以下」とする。曖昧な「十分安定した場合」は使わない。

---

## Q28. トップページのアーカイブ一覧件数とページネーション方針は？
**セクション**: UI.md 3.1
**質問**: トップページに「過去の記事一覧」を掲載する設計だが、表示件数の上限はあるか？記事が数十件・数百件に増えた場合、ページネーション（Jekyll の `paginate` プラグイン等）を導入するか？初期版での上限件数を定義するか？
**回答**: トップページのアーカイブ一覧は最新 20 件まで表示する。20 件を超える場合は `archives.html` へ誘導する。ページネーションは初期必須にせず、記事数 50 件超を目安に導入検討とする。

---

## Q29. OGPタグの設定方針は？
**セクション**: UI.md 全体, Q22回答
**質問**: Q22の回答で「OGP画像が自動で表示されるような仕組み（リンクカード等）は許容する」とあるが、UI.md にはOGPメタタグの仕様がない。Jekyll のテンプレートにOGPメタタグ（og:title, og:description, og:image等）を含めるか？含める場合はトップページのみか全記事ページ共通か？og:imageのデフォルト画像はどこに置くか？
**回答**: 初期版ではトップページと記事ページに共通 OGP を設定する。最低限 `og:title` `og:description` `og:type` `og:url` `og:image` を出力する。`og:image` は相対パスではなく絶対 URL とし、`{{ site.url }}{{ site.baseurl }}/assets/ogp/default.png` を使う。

---

## Q22. X投稿内の画像やメディアの扱いはどうするか？
**セクション**: 5.3, 15.5
**質問**: SPECでは「要約とリンク中心」としているが、元投稿に画像や動画が含まれる場合、サムネイルとして表示するか、あるいは完全にテキストのみとするか？
**回答**: 初期版はテキストのみ（リンク含む）とする。画像の自動取得や埋め込みは著作権および実装コストの観点からPhase 2以降の検討事項とする。ただし、OGP画像が自動で表示されるような仕組み（リンクカード等）は許容する。

---

## Q23. 読者からのフィードバックやコメント機能は導入するか？
**セクション**: 17.4, 24
**質問**: 静的サイト（GitHub Pages）において、GitHub Issuesを利用したコメント欄（Giscus等）や、問い合わせフォームなどの設置予定はあるか？
**回答**: 初期版ではサイト内コメント機能やフォームは設置しない。フッターに SNS（X等）へのリンクを置くが、これは情報共有用であり、読者からの反応は X 上での言及（リプライや引用ポスト）を想定している。サイト内での直接的なインタラクション機能は将来的な拡張案とする。

---

## Q30. GitHub Pages の公開ソースは `docs/` を使うのか？
**セクション**: 23, Q26
**質問**: GitHub Pages の branch source は通常 `/(root)` または `/docs` を前提にする。初期版では `docs/` を正式採用とするか、それとも GitHub Actions で別ディレクトリから公開するか？
**回答**: GitHub Pages の標準公開方式に素直に合わせるため、初期版は `/docs` を採用する。`site/` 維持は GitHub Actions 前提になるため、初期版では採らない。

---

## Q31. 実機確認済み件名と Gmail 検索条件は整合しているか？
**セクション**: 10.2, 10.3, `poc_gmail_read_spec.md` 8.1
**質問**: 実機確認済みの件名は「Claude Code新機能が急上昇」だが、検索条件候補は「件名または本文に `AI` `Grok` `バズ` のいずれか」を前提にしている。PoC の第一検索条件は `from:noreply@x.ai to:garyohosu@gmail.com newer_than:7d` を基本にし、件名キーワード条件は補助条件へ下げるべきではないか？
**回答**: PoC の一次検索条件は `from:noreply@x.ai to:garyohosu@gmail.com newer_than:7d` を正式採用とする。件名条件は補助条件に下げる。`AI` `Grok` `バズ` `Claude Code` は補助検索候補として使うが、必須条件にはしない。

---

## Q32. `data/draft/YYYY-MM-DD.json` の重複保存はどう扱うか？
**セクション**: 20.3
**質問**: 1 日に複数の解析失敗が発生した場合、`data/draft/YYYY-MM-DD.json` では上書きが起きる。ファイル名に時刻や `message_id` を含めるか、日次 JSON へ追記するか、どちらを正式仕様とするか？
**回答**: 正式な命名規則は `data/draft/YYYY-MM-DD_<message_id>.json` とする。`message_id` が使えない場合のみ、`YYYY-MM-DD_HHMMSS` を代替とする。

---

## Q33. raw / processed の保存ファイル名は日付単位か message_id 単位か？
**セクション**: 16.1, 16.2, `fetch_gmail_spec.md` 7.1, `parse_mail_spec.md` 8.1
**質問**: `SPEC.md` では `data/raw/YYYY-MM-DD.json` / `data/processed/YYYY-MM-DD.json` だが、詳細仕様では `YYYY-MM-DD_<message_id>.json` を採用している。2026-03-13 の PoC サンプルでは同日に 2 通取得できており、日付単位ファイル名だと上書きが発生する。どちらを正式仕様とするか？
**回答**: 未回答。`SPEC.md` と詳細仕様の定義を統一する必要がある。

---

## Q34. fetch の正式取得条件は Gmail ラベル必須か、検索クエリ主導か？
**セクション**: 11.2, 11.3, `fetch_gmail_spec.md` 6.1
**質問**: `SPEC.md` 11.3 では「ラベルが付いている」「受信日時が当日または直近24時間以内」を取得条件としている一方、`fetch_gmail_spec.md` はラベルを使わず `newer_than:7d` で検索する仕様になっている。初回運用時に未ラベルの過去メールや 24 時間超のメールを拾ってよいのか、正式条件はどちらか？
**回答**: 未回答。ラベルの必須/任意、および取得対象期間を仕様として一本化する必要がある。

---

## Q35. 本文形式の正式な前提は 1 種類か 2 種類か？
**セクション**: 10.2, 12.3, `parse_mail_spec.md` 2
**質問**: 2026-03-13 の PoC サンプルには、`アイテム 1` / `Title:` 形式の構造ラベル型と、`リンク:` / `(Likes:)` を含む箇条書きまとめ型の 2 通が保存されている。一方 `SPEC.md` 12.3 はラベル抽出を基本方式としており、ニュース単位の分割も `1.` `2.` 前提になっている。正式仕様として 2 形式対応を必須にするか、また構造ラベル型の分割キーは `アイテム N` と `1.` `2.` のどちらを正とするか？
**回答**: 未回答。PoC 実データに合わせて解析前提を再定義する必要がある。

---

## Q36. parse 失敗時の draft 生成責務はどのスクリプトが持つか？
**セクション**: 20.3, `parse_mail_spec.md` 9, 11
**質問**: `SPEC.md` 20.3 では解析失敗時に `data/draft/YYYY-MM-DD_<message_id>.json` を保存するとしているが、`parse_mail_spec.md` には draft 保存処理がなく、`parse_status` 更新とフォールバック処理のみが定義されている。draft を生成する責務は `parse_mail.py` か、後段の `compose_article.py` か、あるいは `main.py` か？
**回答**: 未回答。draft 作成の判定条件と責務分界を明示する必要がある。

---
